[project]
name = "bitcoin-quant-research-lab"
version = "0.1.0"
description = "Autonomous Bitcoin pivot research, backtesting and AI experimentation laboratory."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116",
  "uvicorn[standard]>=0.35",
  "httpx>=0.28",
  "pydantic>=2.11",
  "pydantic-settings>=2.10",
  "polars>=1.32",
  "duckdb>=1.3",
  "numpy>=2.2",
  "python-dotenv>=1.1",
  "typer>=0.16",
  "rich>=14.1",
]

[project.optional-dependencies]
dev = ["pytest>=8.4", "ruff>=0.12"]

[project.scripts]
bqrl = "btc_quant_lab.cli:app"
bqrl-detector-research = "btc_quant_lab.ai.detector_research_cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/btc_quant_lab"]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint.per-file-ignores]
# These modules intentionally catch failures at AI-generated-code / external-model boundaries
# and convert them into conservative rejections instead of crashing a research run.
"src/btc_quant_lab/ai/research_loop.py" = ["BLE001"]
"src/btc_quant_lab/ai/detector_research_loop.py" = ["BLE001"]
"src/btc_quant_lab/ai/sandbox.py" = ["BLE001"]

[tool.pytest.ini_options]
pythonpath = ["src"]
